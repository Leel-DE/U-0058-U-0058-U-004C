# Competition Radar Automation Hub project index

| Subsystem                | Purpose                                                                                                                     | Source of truth                                                                    | Entry points                                                                      | Validation                                                                    |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| Web control plane        | Authenticated Next.js UI, Server Actions, operations controls, analytics, health, and Inngest functions                      | `apps/web`                                                                         | `src/app`, `src/server/actions`, `src/server/automation`, `src/lib/inngest/functions.ts` | `pnpm.cmd --filter @cr/web typecheck`, `pnpm.cmd --filter @cr/web test`       |
| Automation runtime       | Durable queue consumer, organization pause/schedule/concurrency policy, shared browser, leases, cancellation, recovery, logs, health, and TorqueCore bridge | `apps/worker/src/automation`, `automation_settings`                                | `runtime-supervisor.ts`, `adaptive-scheduler.ts`, `browser-job-executor.ts`       | worker tests/typecheck plus policy-aware queue smoke                          |
| Scraper worker           | Fastify service for Cheerio, shared Playwright fetching, discovery, selector repair, and manual sessions                    | `apps/worker/src`                                                                  | `server.ts`, `fetcher/playwright.ts`, `automation/competition-handlers.ts`        | `pnpm.cmd --filter @cr/worker typecheck`, `pnpm.cmd --filter @cr/worker test` |
| Database                 | Drizzle schema plus raw SQL for RLS, views, storage, verification, and operational tables                                   | `packages/db`                                                                      | `src/schema/index.ts`, `src/apply-sql.ts`, `src/verify-schema.ts`                 | `pnpm.cmd db:verify`, `pnpm.cmd typecheck`                                    |
| Shared contracts         | Cross-package DTOs, Zod schemas, constants, and result types                                                                | `packages/shared/src`                                                              | `schemas`, `types.ts`, `constants.ts`                                             | `pnpm.cmd --filter @cr/shared typecheck`, `pnpm.cmd --filter @cr/shared test` |
| Background orchestration | Inngest schedules typed jobs; Postgres owns execution leases, organization policy, retries, progress, and dead letters      | `apps/web/src/lib/inngest/functions.ts`, `packages/db/sql/0014_automation_hub.sql`, `0015_automation_controls.sql` | `/api/inngest`, `automation_jobs`, `automation_settings`                         | web tests plus transactional pause/concurrency smoke                          |
| Local runtime            | Electron tray app supervises hidden web, worker, and Inngest processes with recovery and encrypted optional credentials     | `apps/desktop/src/main.mjs`                                                        | `pnpm.cmd dev:desktop`, tray app                                                  | tray state plus worker health and runtime page                                |
| Shipment automation      | Fixed provider adapters, deterministic consensus, adaptive schedule, user-friendly notification, and live TorqueCore bridge | `apps/worker/src/automation`                                                       | `/shipments`, shipment actions, `shipment-handler.ts`, `shipment-result-store.ts` | worker tests/typecheck/build plus real queued shipment run                    |

See `AUTOMATION_HUB.md`, `ARCHITECTURE.md`, `INTEGRATIONS.md`, `DATABASE.md`, `VALIDATION.md`, and `RISKS_AND_DEBT.md` before architecture-changing work.
